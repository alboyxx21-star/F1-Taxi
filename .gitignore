# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Stray binaries / downloads that ended up in the folder (not part of the site)
*.exe
*.winmd
dl-*.jpg
/ffmpeg/

# ---- Design source material (kept locally, not part of the deployed site) ----
# All working photos, palette refs and design prototypes live here; the deploy
# copies used by the site live in assets/media/.
/_design-source/

# Deployment bundle (upload to hosting, don't commit)
*.zip
