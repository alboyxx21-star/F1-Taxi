/* ============================================================
   F1 TAXI — Google Maps key for the embedded calculator map.

   Paste a BROWSER key here to turn the Çmimet calculator into a
   live Google map with route drawing. Leave it empty ("") and the
   calculator keeps working with the free OpenStreetMap fallback.

   Restrict this key in Google Cloud Console:
     • Application restriction: HTTP referrers → your domain
         https://yourdomain.com/*   (+ your local dev URL)
     • API restriction: Maps JavaScript API, Places API, Directions API
   The referrer lock makes the key useless on any other site.
   (Do NOT commit the filled-in key to a public repo.)
   ============================================================ */
window.F1_MAPS_KEY = "";
